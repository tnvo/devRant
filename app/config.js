const config = require('electron-config');

module.exports = new config({
    defaults: {
        lastWindowState: {
            x: 0,
            y: 0,
            width: 450,
            height: 912
        }
    }
});
